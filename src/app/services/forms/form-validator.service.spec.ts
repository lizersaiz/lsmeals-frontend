import { FormValidatorService } from './form-validator.service';

describe('InputValidator', () => {
  it('should create an instance', () => {
    expect(new FormValidatorService()).toBeTruthy();
  });
});
